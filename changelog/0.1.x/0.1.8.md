---
summary: "Code simplification: shared parseDateParts/formatDateParts/normalizeFunderId helpers; mcp-ts-core ^0.9.6 → ^0.9.9; ambiguous_journal error code InvalidParams → ValidationError"
breaking: false
security: false
---

# 0.1.8 — 2026-05-24

## Changed

- **`crossref_search_journals`**: `ambiguous_journal` error code corrected from `InvalidParams` to `ValidationError` — the condition (ambiguous title match) is a client-recoverable validation issue, not an invalid parameter.
- **`crossref-service.ts`**: extracted shared `parseDateParts`, `formatDateParts`, and `normalizeFunderId` helpers — four tool-file copies of date parsing consolidated into one, two copies of funder ID stripping consolidated into one (net −20 lines across tool definitions).
- **`@cyanheads/mcp-ts-core`** ^0.9.6 → ^0.9.9 — picks up `git-wrapup` skill, `ctx.fail`/`ctx.notifyPromptListChanged`/`ctx.notifyToolListChanged` docs, fuzz Phase 1 pre-parse fixes, `fast-check` moved to optional peer dep, structured tag annotation guidance, and release artifact verification step.
- **`CLAUDE.md` / `AGENTS.md`**: framework version reference updated; "Close the loop on issues" core rule added; `ctx.fail`, `ctx.notifyPromptListChanged`, `ctx.notifyToolListChanged` added to Context table; `git-wrapup` skill added to skills table.
- **Skills**: `git-wrapup` v1.0 added; `release-and-publish` updated v2.5 → v2.6 (artifact verification step, `server.json`/`manifest.json` distinction clarified, GHCR 401/403 auth guidance).
- **`changelog/template.md`**: tag annotation format example added (structured markdown sections, dep arrows, test footer).
